class Main{
	mid : Int;

	main(): Int {
	    case mid of
	    	x : Int => (1+1+1*9);
	    	y : Bool => (1+2);
	    esac
	};

	foo(): Int {
	    case mid of
	    	x : Int2 => (x+1);
	    	y : Bool => (1+2);
	    esac
	};
};